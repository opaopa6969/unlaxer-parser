package org.example.snapshot;

import java.io.IOException;
import org.eclipse.lsp4j.debug.launch.DSPLauncher;
import org.eclipse.lsp4j.debug.services.IDebugProtocolClient;
import org.eclipse.lsp4j.jsonrpc.Launcher;

@javax.annotation.processing.Generated("org.unlaxer.dsl.codegen.DAPLauncherGenerator")
public class SnapshotDapLauncher {

    protected SnapshotDebugAdapter createAdapter() {
        return new SnapshotDebugAdapter() {};
    }

    public static void main(String[] args) throws IOException {
        new SnapshotDapLauncher().launch();
    }

    public void launch() throws IOException {
        SnapshotDebugAdapter adapter = createAdapter();
        Launcher<IDebugProtocolClient> launcher =
            DSPLauncher.createServerLauncher(adapter, System.in, System.out);
        adapter.connect(launcher.getRemoteProxy());
        launcher.startListening();
    }
}
