[package]
name = "unlaxer-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dev-dependencies]
stats_alloc = "0.1"

# Allocation counting is process-wide, so this test runs its scenarios sequentially
# without the multi-threaded libtest harness.
[[test]]
name = "checkpoint_allocation"
harness = false
